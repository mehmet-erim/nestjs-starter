{
  "type": "postgres",
  "host": "localhost",
  "port": 5432,
  "username": "postgres",
  "password": "",
  "database": "postgres",
  "entities": ["src/**/**.entity{.ts,.js}"],
  "synchronize": false,
  "migrations": ["migration/*.ts"],
  "logging": true,
  "cli": {
    "migrationsDir": "migration"
  }
}
